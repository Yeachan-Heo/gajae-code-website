name: Disabled legacy version sync

on:
  workflow_dispatch:

permissions: {}

jobs:
  disabled:
    name: Superseded workflow notice
    runs-on: ubuntu-22.04
    timeout-minutes: 2
    steps:
      - name: Report replacement workflow
        run: "echo 'disabled: superseded by sync-release.yml'"